apiVersion: v1
kind: Secret
metadata:
  name: {{ include "poc-app.fullname" . }}
  labels:
    {{- include "poc-app.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
